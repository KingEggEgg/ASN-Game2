# ASN Rush Live Leaderboard - Firebase Connected Build

This build is already connected to Firebase project `asn-rush-commercial-day`.

Before public use, confirm the contents of `firebase-rules.json` have been published under:
Realtime Database -> Rules.

Use:
- `index.html` for the game
- `leaderboard.html` for the live big-screen Top 10 leaderboard
